{
  "name": "@make-me-a-cup-of-tea/quarterback-db",
  "version": "0.0.3",
  "private": true,
  "author": "Atypon Systems LLC",
  "license": "Apache-2.0",
  "homepage": "https://github.com/Atypon-OpenSource/manuscripts-quarterback",
  "main": "generated/index.js",
  "types": "generated/index.d.ts",
  "files": [
    "prisma/*"
  ],
  "scripts": {
    "prod:init": "prisma generate && prisma migrate deploy && pnpm run cp-files",
    "prod:migrate": "prisma migrate deploy --schema=./prisma/schema.prisma",
    "cp-files": "mkdir -p ../api/.prisma && cp ./generated/schema.prisma ../api/.prisma && cp ./generated/index.d.ts ../shared/prisma",
    "build": "prisma generate && pnpm run cp-files",
    "format": "prisma format",
    "generate": "prisma generate && pnpm run cp-files",
    "migrate": "prisma migrate dev && pnpm run cp-files",
    "delete": "prisma migrate reset",
    "reset": "prisma migrate reset && pnpm run cp-files"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "prisma": "3.1.1",
    "ts-node": "^10.4.0",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "@prisma/client": "3.1.1"
  }
}
